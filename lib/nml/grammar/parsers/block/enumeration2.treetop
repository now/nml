module NML::Grammar::Parsers::Block
  grammar Enumeration2
    include Item2

    rule enumeration2
      ('  ' ('₁' / '₂' / '₃' / '₄' / '₅' / '₆' / '₇' / '₈' / '₉' / '₀') '   ' item:item2)+
      <NML::Grammar::Nodes::Block::Enumeration>
    end
  end
end
